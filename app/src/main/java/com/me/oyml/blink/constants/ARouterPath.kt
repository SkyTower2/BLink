package com.me.oyml.blink.constants

class ARouterPath {
    companion object {
        private const val MAIN = "/main"
        private const val DEVICE = "/device"
        const val DEVICE_DETAIL = "$DEVICE/detail"
        const val MAIN_HOME = "$MAIN/home"
    }
}